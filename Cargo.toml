# Minimal Cargo.toml to satisfy PMAT compliance checker
# This is a Python project - see pyproject.toml for actual dependencies
[package]
name = "hf-hub-ecosystem-stub"
version = "0.0.0"
edition = "2021"
rust-version = "1.70"
publish = false

[lib]
path = "stub.rs"
